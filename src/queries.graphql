type Query {
  getBetList: [Bet]
}

type Query {
  getBet(id: ID!): Bet
}

type Query {
  getBestBetPerUser(limit: Int!): [Bet!]
}

type Query {
  getUser(id: ID!): User
}

type Query {
  getUserList: [User]
}
