type Query {
  getBetList: [Bet]
}

type Query {
  getBet(id: ID!): Bet
}

type Query {
  getBestBetPerUser(limit: Int!): [Bet!]
}

type Mutation {
  createBet(userId: Int, betAmount: Float, chance: Float, payout: Float): Bet
}

type Query {
  getUser(id: ID!): User
}

type Query {
  getUserList: [User]
}
